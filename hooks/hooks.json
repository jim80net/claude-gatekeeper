{
  "description": "Evaluates PCRE2 regex rules against tool calls. Deny always wins.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/claude-gatekeeper",
            "timeout": 10,
            "statusMessage": "Checking permissions..."
          }
        ]
      }
    ]
  }
}
